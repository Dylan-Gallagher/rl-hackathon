"""path4.coldstart — cold-start SFT pipeline over alloy success traces.

Pipeline position: alloy (Path 2) → success traces → **cold-start SFT** →
EI (Path 3) → GRPO (Path 4 veRL side, `path4.verl_grpo`).
"""
